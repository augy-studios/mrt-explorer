# css

- `theme.css`: the uwuapps theme system, verbatim from `uwuapps-theme.md`
  section 1. Change it only when that spec changes.
- `style.css`: layout and components for this app. Colours come from theme
  tokens only. Line colours arrive as `--line` and `--line-ink` custom
  properties set by `js/lines.js`.
