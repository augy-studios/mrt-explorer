# images

Screenshots referenced by `manifest.json` for the install prompt:
`screenshot_1.png` (narrow, 1080x2340) and `screenshot_2.png` (wide,
1920x1080). Keep the sizes in the manifest in step with the files.
